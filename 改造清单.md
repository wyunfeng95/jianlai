# 《武侠江湖模拟器》框架改造清单

面向 `武侠江湖模拟器.html`（4033 行 / 491KB 单文件，含 3 段 script）。
三大改造方向：**换题材**、**换 API**、**加新子系统**。

> 行号会随编辑漂移：**一律以「符号名锚点」为准**，行号只用于初次定位。
> 编辑顺序建议：①换 API 驱动层 → ②换题材文案与词汇 → ③加子系统 → ④改存档版本 → ⑤跑回归。

---

## 0. 改造前必须建立的认知：哪些能改，哪些一改就崩

| 类别 | 内容 | 处置 |
|---|---|---|
| 文案 / 词汇 / 名词 | `WORLD_RULES`、`STYLE_SYSTEM`、`NPC_SCHEMA`、各 `*Prompt` 模板、`FREEDOM[].tone`、UI 里的中文 | **全量替换**，这是换题材的主战场 |
| 裁定与账本 | `makeJudge`、`rollCheck`、`applyTurn`、`advanceTime`、`ledger`、`endDuel`、`convoCaps`/`applyConvoEffects` | **保留结构，只换数值口径与文案**。它们保证“模型改不了判定” |
| 反幻觉防线 | `fixSelfRef`/`fixNames`（姓名一致性）、`AV_DESC`→提示词回灌（相貌一致性）、`plain`/`asArr`/`normSkills`（字段防呆）、`parseJSONLoose`/`repairJSON` | **必须保留**。这四项是长局不崩的主因 |
| 记忆分层 | `MEM_PRESET` + `foldHistory`→`volumes` + `ledger` | **保留三件套**。换题材只改压缩提示词 `volumePrompt` |
| 仅具象但可替换 | 头像雪碧图 `AV_SHEET`/`AV_SLOTS`/`AV_DESC`、`GAN/ZHI/MONTH_NAMES`、`RK_SUR/RK_GIVEN`、`INJURIES`、`STANCES/TACTICS` | **按新题材重建数据表**，代码逻辑不动 |
| 存储键 | `LS_CFG`/`LS_SAVE`/`LS_HALL`/`LS_SLOT`/`IDB_NAME` | 换题材换命名空间，避免与旧档串味 |

---

## 1. 锚点总表（按模块）

| 模块 | 符号 | 用途 |
|---|---|---|
| 配置 | `LS_CFG` `LS_SAVE` `LS_HALL` `LS_SLOT` `IDB_NAME` `IDB_STORE` | 存储命名空间 |
| 状态 | `SAVE_VERSION` `newStateShell()` `migrate(s)` | 存档骨架与版本迁移 |
| 显示器 | `dateOf/dateStr` `GAN` `ZHI` `MONTH_NAMES` `dateOf` | 年月显示 |
| 头像 | `AV_COLS/AV_ROWS/AV_SLOTS/AV_SHEET/AV_DESC/AV_AGE` `avSlotOf` `avPickSlot` `avRing` `avatarFace` `npcPortrait` | 类型化头像与相貌回灌 |
| 姓名 | `COMPOUND_SURNAMES` `SURNAME_CHARS` `surnameOf` `fixSelfRef` `fixNames` | 姓名一致性 |
| 生计 | `upkeepPerMonth` `advanceTime` `ageNote` `growthCap` `lifespanOf` `grantLifespan` `agingTick` | 时间与开销 |
| 难度/自由度 | `DIFF` `diff()` `FREEDOM` `fdm()` `fillRules` `worldRules` `worldHead` | 双轴调节 |
| 判定 | `d20` `rollMod` `fateInfo` `rollCheck` `attrVal` `makeDoom` `doomText` | 骰与判定 |
| 武学 | `ART_STYLES` `STYLE_OF_STANCE` `STYLE_DESC` `normArt` `artFor` `artBonus` `weaponBonus` `REALMS` `realmOf` `powerLevel` `wuNorm` `WCAP` `clampW` `manualMeta` `studyRoll` `studyManual` | 招式与境界 |
| 门派 | `SECT_RANKS` `sectRank` `sectNext` `syncSect` `sectStipend` `sectTick` | 组织位阶 |
| 伤残仇怨 | `INJURIES` `pickInjury` `sufferInjury` `hasScar` `addVendetta` `tickVendettas` `kinOf` | 负面后果链 |
| 榜单 | `RK_SUR` `RK_GIVEN` `RK_NOTE` `rankFloor` `makeRankName` `rankCatchUp` `rankRefresh` | 世界新陈代谢 |
| 世界事件 | `WORLD_EVENTS` `pickWorldEvent` `news` `ledger` | 引擎插话与台账 |
| 提示词 | `WORLD_RULES` `STYLE_SYSTEM` `JSON_RULE` `NPC_SCHEMA` `OPTIONS_RULE` `initSchemaPrompt` `worldPrompt` `stateBlocks` `judgeBlock` `turnPrompt` `duelAftermathPrompt` `convoPrompt` `volumePrompt` `bioPrompt` | 全部发包内容 |
| LLM | `callLLM` `extractPartialField` `parseJSONLoose` `repairJSON` `llmJSON` `lastFinish` | 驱动层 |
| 回合 | `act` `runTurn` `makeJudge` `applyTurn` `applyInit` `applyWorld` `gameOverFlow` `fallbackOptions` `optMonths` `optChance` | 主循环 |
| 比武 | `STANCES` `TACTICS` `ACTIONS` `actionCost` `findAmmo` `oppProfile` `oppStance` `power` `oppBonus` `duelRound` `startDuel` `endDuel` `afterDuel` `showWinChoice` `snapshotDuel` `restoreDuel` | 战斗子系统 |
| 对话 | `convoPrompt` `openConvo` `convoSend` `endConvo` `convoCaps` `applyConvoEffects` `snapshotConvo` `restoreConvo` | 对话子系统 |
| 面板 | `renderPanel` `renderBag` `renderPeople` `renderGraph` `renderWorld` `renderHall` `renderOptions` `renderSlots` | UI |
| 成就评分 | `ACHIEVEMENTS` `checkAchievements` `scoreOf` `titleOf` `playerRank` `rankText` | 终局 |
| 定稿 | `logChapter` `bookPut` `bookAll` `bookClear` `chapterText` `exportBook` `saveGame` `loadGame` `flushAll` | 存档与导出 |

---

## 2. 换题材

### 2.1 词汇映射表（改这张表就完成 70% 的换皮）

按目标题材替换下列**贯穿全代码的词汇**。注意：属性名是**中文字符串当键名**用的（`p.attributes['武功']`、`NPC_SCHEMA` 里的 `"武功"`、`applyTurn` 里的 `ch.attributes` 遍历），改名必须**全代码同步**且**旧档要迁移**。

| 武侠原名 | 仙侠 | 都市异能 | 星际佣兵 | 校园 |
|---|---|---|---|---|
| 武功 | 修为 / 灵力 | 异能等级 | 战力评级 | 学力 |
| 谈吐 | 道心 / 神念 | 魅力 | 交涉 | 口才 |
| 才学 | 悟道 | 学识 | 战术素养 | 学业 |
| 颖悟 | 灵根 | 天赋 | 神经反射 | 天资 |
| 气血 | 生机 | 体力 | 生命体征 | 精神 |
| 侠名/恶名 | 道誉/魔名 | 声望/通缉度 | 威望/恶名 | 风评 |
| 家财（两） | 灵石 | 存款（元） | 信用点 | 零花钱 |
| 内力（Qi） | 真元 | 能量 | 电池 | 精力 |
| 武学/招式 | 法术/神通 | 异能/技能 | 战术动作 | 特长 |
| 路数（刚猛/守御/诡变/绝学/身法） | 五行（火/土/幻/禁/风） | （力量/防御/诡术/爆发/机动） | （突击/防御/战术/重武器/机动） | （应试/防守/取巧/爆发/交际） |
| 秘籍 | 道书/传承玉简 | 机密档案 | 数据芯片 | 复习资料 |
| 寿元 | 道寿 | 寿命 | 服役年限 | 年纪 |
| 门派 | 宗门 | 组织/财团 | 军团/公司 | 社团 |
| 江湖榜 | 天机榜 | 悬赏榜 | 战力排行 | 成绩榜 |
| 仇家/heat | 仇家/因果 | 仇家/仇恨值 | 通缉/追杀值 | 对头 |
| 闭关参悟 | 打坐悟道 | 特训 | 模拟训练 | 备考 |
| 走火入魔 | 心魔反噬 | 异能失控 | 神经崩解 | 崩溃 |
| 往生录 | 天人五衰录 | 讣告 | 阵亡名单 | — |
| 传/回合/回 | 章/纪 | 篇/章 | 周期/轮 | 学期/周 |
| 立传/墓志铭 | 道藏本纪/碑铭 | 档案/结语 | 战报/悼词 | 毕业册/留言 |

**必改副作用**：
- `titleOf(p)` 的称号词（无名之辈→一代大侠→宗师）；`REALMS`（不入流→三流→…→神话）；`SECT_RANKS`（记名弟子…掌门）。
- `fateInfo` 的「大凶/不顺/平常/顺遂/大吉」与 `judgeBlock` 里「本回合虽是大凶…」的措辞。
- `fallbackOptions()` 与 `WORLD_EVENTS`、`INJURIES`、`RK_NOTE`、`HIT_TXT/BIG_TXT/MISS_TXT` 五张语料表。
- UI 字符串：`toast` 提示、`setBusy('笔走龙蛇，正在推演江湖……')`、章节标记「传」、`chapmark`、导出的 markdown 表头、`<title>`/`theme-color`/manifest 名称。

### 2.2 世界观铁律：`WORLD_RULES`（锚点唯一，替换整块）

结构照抄，**14 条铁律的顺序与硬口气全部保留**（这是压制模型胡说的地方）：

1. 世界观边界（原：无朝廷官家、禁科幻玄幻）
2. 文风（笔法 + 对话引号「」+ 称呼体系）
3. 姓名铁律（面板为准，不可改名——**保留**）
4. 属性规则（数值 0-100 + 增长上限）
5. 体系制度（原“武学制度”：路数枚举 + style 必填）
6. 判定制度（骰面已定，不可篡改成败）
7. 战斗制度（**不要在剧情里写出胜负**，写到动手为止 + `duel` 字段）
8. 组织制度（原“师门规则”：贡献度、位阶、叛出代价）
9. 排行制度（原“江湖榜规则”：只认打出来的名次）
10. 后果制度（伤残 + 记仇）
11. 好感度规则（原表是**精确数值表**：-10/-20/-40/-50/归零——换成新题材时必须重写同等精度的表，不要留形容词）
12. 情感规则（好感门槛 `{{LOVE}}` → 爱恋值重置 30 → 70 求婚 / 出轨 -50）
13. NPC 规则（完整面板、画像回灌、secret、npcEvents 人生线）
14. 长短线（宿命 quests）+ 生计光阴（月推进、禁时间跳跃、横财克制）+ 进程规则（不替玩家选择、不擅自结局）

**替换要点**：第 5、6、7 条对应新题材的核心对抗系统（仙侠的斗法、都市的异能对决、星际的战术交锋），**枚举值必须与 §4 的战斗子系统数据表一致**，否则模型给的 `style` 会全部 fallback。

### 2.3 自由度三档：`FREEDOM`（锚点唯一）

每档 20+ 个字段必须**逐个重新定标**，不能只改 label：

| 字段 | 含义 | 换题材时怎么定 |
|---|---|---|
| `wcap` | 属性上限 | 100（写实）→ 1000（爽档） |
| `lifeCap` / `lifeGift` | 寿命上限 / 每次奇遇上限 | 决定长篇能玩多久 |
| `check` | 判定气运加成 | 直接加到玩家属性上，收益最大的一档 |
| `growth` `money` `upkeep` `starve` | 成长/收入/开销/饥寒系数 | 决定经济曲线 |
| `doom` `injury` `kill` `aging` `vendetta` | 惩罚系数（0=关闸） | 0 即“世界不跟你较真” |
| `studyDC` `studyHurt` | 参悟难度修正 / 受伤系数 | 负值=更容易 |
| `freeAct` | 玩家自述行动一律算做成 | 叙事口径开关 |
| `storyDeath` | 剧情杀闸门（**见 `applyTurn` 末尾的判定**） | 0=模型写死主角也会被拉回来，只落重伤 22 血 |
| `secretGate` `loveGate` | 秘密/求爱门槛 | 换成新题材的“关键信息门槛” |
| `chosen` | 填 `{{CHOSEN}}` | 一句话世界态度 |
| `tone` | 每回合注入的叙事口径 | 该档的行为准则，**改这里比改铁律更有效** |
| `convo` | 只注入对话的额外口径 | 爽档写“主动想帮他并直接写进 effects” |

`DIFF`（逍遥/江湖/修罗）只动 `dc` 与 `opp` 两个数，与 `FREEDOM` **正交**——保持这个正交设计，不要合并。

### 2.4 文风：`STYLE_SYSTEM`

两段“范例”是收敛文风最便宜的手段：换题材时**重写范例**（一段日常、一段冲突），并在末尾保留「叙述一律用中文；除 JSON 键名外不得出现英文」这条硬约束。

### 2.5 数据表重建清单

| 数据表 | 行数级别 | 换题材要做的事 |
|---|---|---|
| `AV_SLOTS` + `AV_DESC` + `AV_SHEET` | 56 槽 | 重新出图：8×7 雪碧图 + `性别+年龄段+特殊身份` 编码（`m1..m4`/`f1..f4`/`x_*`），`AV_AGE` 同步。**槽位语义变了必须同步改 `avPickSlot`/`avBucket`** |
| `GAN/ZHI/MONTH_NAMES` | 10/12/12 | 仙侠可换纪元+节气；科幻换星历；现代换年月 |
| `RK_SUR/RK_GIVEN/RK_NOTE` | 数百/30/12 | 新题材的姓氏库与成名语 |
| `INJURIES` | 数条 | 新题材的永久性后果（伤残→灵根受损/异能失控/债务/污名） |
| `WORLD_EVENTS` | 数条 | 世界级插曲，带权重字段则照抄结构 |
| `SKILL_WORDS` | 4 组正则 | 把「精通/擅长/略通/皮毛」的文本→数值映射换成新题材说法 |
| `BACKGROUNDS` | 数条 | 出身选择项（`[名称, 描述, ...]`），与 `initSchemaPrompt` 的出身随机池对应 |
| `ACHIEVEMENTS` | 22 条 | `[id, 名称, 条件函数, 描述]`，条件函数直接吃 `S` |
| `STANCES/TACTICS` | 5+3 | 见 §4 |

---

## 3. 换 API

### 3.1 现状：只有一处出网（`callLLM`）

发包实体固定为 OpenAI 兼容：

```js
{ model, messages:[{role:'system',content:STYLE_SYSTEM},{role:'user',content:prompt}],
  temperature:1.05, max_tokens:12000, stream:true, response_format:{type:'json_object'} }
if(/deepseek/i.test(body.model)) body.thinking={type: cfg.think?'enabled':'disabled'};
// 鉴权：Authorization: Bearer cfg.key
// 端点：cfg.base.replace(/\/+$/,'') + '/chat/completions'
```

流式解析只认 SSE 文本行 `data: {...}` 里的 `choices[0].delta.content` 与 `choices[0].finish_reason`；`finish_reason==='length'` 被 `llmJSON` 用来触发“精简重发”。

### 3.2 目标：把 `callLLM` 改成适配器注册表

**第 1 步**：在配置旁新增适配器表（照抄现有风格，零依赖）：

```js
// —— 适配器接口约定 ——
// build(cfg, prompt, opt)      -> {url, headers, body}         (或返回 Request)
// parseSSE(line, j)            -> {text, finish}               (j=JSON.parse(payload) 后的对象)
// extractError(resp)           -> Promise<string>
// nonStream(cfg, prompt, opt)  -> {url, headers, body}         (无流式时的整包请求)
const ADAPTERS = {
  openai: { /* 默认，DeepSeek/OpenAI/兼容网关 */ },
  anthropic: { /* /v1/messages, system 单独字段, x-api-key, anthropic-version */ },
  gemini: { /* :streamGenerateContent?alt=sse, contents[].parts[].text */ },
  ollama: { /* http://localhost:11434/api/chat, NDJSON 非 SSE */ },
  mock: { /* 本地假模型：按 prompt 关键字返回预制 JSON，供离线测框架 */ },
};
```

**第 2 步**：`callLLM` 内只保留与协议无关的逻辑（超时 `bump()`、`AbortController`、累积 `raw`、`onPartial` 回调、`lastFinish`），协议差异全部下沉到适配器：

| 项 | OpenAI 兼容 | Anthropic | Gemini | Ollama |
|---|---|---|---|---|
| 端点 | `/chat/completions` | `/v1/messages` | `:streamGenerateContent?alt=sse` | `/api/chat` |
| 鉴权 | `Authorization: Bearer` | `x-api-key` + `anthropic-version` | `?key=` | 无 |
| system | `messages[0].role='system'` | 顶层 `system` | `systemInstruction` | `messages[0].role='system'` |
| JSON 强约束 | `response_format:{type:'json_object'}` | 无 → 靠 `JSON_RULE` + `repairJSON` | `generationConfig.responseMimeType='application/json'` | `format:'json'` |
| 思考开关 | `thinking:{type:'enabled'}` | `thinking:{type:'enabled',budget_tokens}` | `thinkingConfig` | 无 |
| 流式行 | `data: ` SSE | `data: ` SSE（事件名不同） | `data: ` SSE | NDJSON 每行一个对象 |
| 文本路径 | `choices[0].delta.content` | `delta.text`（`content_block_delta`） | `candidates[0].content.parts[0].text` | `message.content` |
| 结束因 | `finish_reason` | `stop_reason` | `finishReason` | `done` |

**第 3 步**：`llmJSON` 的重试策略改为按适配器能力分级：

```js
// 能力探测（每个适配器声明）
caps = { nativeJSON:true, stream:true, think:false, maxTokensField:'max_tokens' }
// 无 nativeJSON 的适配器：把 JSON_RULE 加强为「只输出 JSON」并将其放在 prompt 末尾
// 重试三档（保留现有语义）：
//   ① 原样重发（网络抖动）
//   ② finish_reason==='length' → 追加精简指令（现有文案可复用）
//   ③ 格式错 → 追加严格 JSON 指令；连续两次失败再降 max_tokens 30% 重发
```

### 3.3 换 API 的检查清单

- [ ] `cfg` 扩字段：`{base, key, model, think, provider, temperature, maxTokens, timeout, stream}`；`saveCfgFromFields`/`openSettings`/`cfgSave` 三处同步；加 `provider` 下拉。
- [ ] 设置面板（`settingsMask` 区块）加：provider、temperature、超时秒数、**「测试连接」按钮**（发一句 `{"ok":1}` 的最小请求，回报延迟与是否支持 JSON 模式）。
- [ ] 密钥不进导出存档：`exportBook`/`svExportJson` 只导 `S`，确认 `cfg` 不在 `S` 内（现状安全，改造后别把 cfg 塞进 S）。
- [ ] 跨域：非 OpenAI 兼容端点常需代理；在设置里给出「自备代理 base」说明，别在代码里硬编码代理。
- [ ] 成本与用量：在 `callLLM` 返回处接 `usage`（若适配器提供），累加进 `S.stats.tokens` 并在面板显示，便于长局控费。
- [ ] 错误文案统一走 `toast` + 可重试按钮（`runTurn` 的 catch 分支已有此结构，复用）。
- [ ] `mock` 适配器务必实现：给出 5 个预制响应（开局 / 普通回合 / 比武余波 / 对话 / 立传），让主题改造与子系统开发**离线可跑**。

---

## 4. 加新子系统

### 4.1 通用四步模板（任何新子系统都照这个来）

新增一个子系统（例：**炼丹 / 义体改造 / 灵宠 / 商队经营**）需改 7 处，缺一处就会出现“引擎不认账”或“模型乱给”：

```
① 状态：newStateShell() 加字段 + migrate() 加 s.v<7 分支 + SAVE_VERSION=7
② 裁定：makeJudge(opt) 里为新选项类型掷骰（opt.type 新增枚举）
③ 结算：applyTurn(d, action, judge) 里读模型返回的新字段，逐条限幅入账（照抄 itemsAdd 的写法）
④ 提示词：新增独立段落常量 XXX_RULE，插进 turnPrompt / judgeBlock 对应位置
⑤ 输出契约：turnPrompt 的「输出格式」加新字段（模型才知道能填）
⑥ UI：renderPanel / renderBag / renderWorld 加一块 + 在 #tabs 加页签（若需要）
⑦ 闭环：ACHIEVEMENTS 加 2-3 条；scoreOf() 加权重；ledger() 记既成事实
```

### 4.2 子系统 A：把「比武」换成新题材的战斗系统

比武是最完整的子系统，改造它等于换掉半个游戏。**必须同步改的锚点**：

| 锚点 | 改什么 |
|---|---|
| `STANCES` `TACTICS` `ACTIONS` `actionCost` | 招式表：`{name,desc,cost}`。新题材例：蓄力/格挡/诡技/爆发/撤离 + 治疗/道具/远程 |
| `STYLE_OF_STANCE` `ART_STYLES` `STYLE_DESC` `ART_STYLES` | 招式路数枚举，**必须与 `WORLD_RULES` 第 5 条、`NPC_SCHEMA`、`normArt` 校验一致** |
| `findAmmo` | 远程/道具消耗品的查找正则（现在是 `镖|针|钉|箭|飞刀|蒺藜|石灰|沙|弹`） |
| `oppProfile` | 从 `personality` 文本正则推对手性格 → 改成新题材的性格标签集合 |
| `oppStance` | 性格→出招权重表 `{A:.40,D:.32,F:.22,S:0,R:0}` |
| `power` `oppBonus` `duelRound` | 伤害分支表（8 个 `else if`）。**保留“招对招各有公式”的结构**，只换系数与文案 |
| `HIT_TXT/BIG_TXT/MISS_TXT` `dmgTxt` | 打击描写语料 |
| `startDuel` | 初始气血/内力公式：`pQi=clamp(50+wuNorm(武功)/2)`、`oppW=clampW(opp.武功+diff().opp)` |
| `endDuel` | 认输/击杀/伤残概率链（`kill` 由邪道+好感+lethal+切磋+难度+自由度连乘） |
| `showWinChoice` | 胜后处置（留手/重创/杀）→ 新题材的等价处置 |
| `duelAftermathPrompt` | 「比武实录」的续写提示词；`judge.duel=true` 让 `applyTurn` **跳过气血结算**（关键，别忘） |
| `duelState` 快照 | 关页面恢复；新状态字段要进 `snapshotDuel`/`restoreDuel` |

**保留不动的核心机制**（这是这个框架最值钱的部分）：胜负由引擎打完 → 只把 `duel.log.plain` 交给模型补写 → 模型返回的 `duel` 必须为 null、气血不许重写。

### 4.3 子系统 B：对话的「实效化」扩展

`convoCaps(npc)` 是额度表，`applyConvoEffects(npc, eff, attemptOk)` 是逐条限幅器。新增可谈成的资源，**只改这两处 + `convoPrompt` 的 effects 字段清单**：

```js
// convoCaps 加字段（照现有 mul/ fav 分档风格）
return { ..., 传授炼丹: fav>=70?1:0, 引荐宗门: f.check>=25?45:60, 情报等级: fav>=60?3:1 };
// applyConvoEffects 加分支：先判 risky(fav 门槛)→ 再限幅 → 最后 gain(out, '…') + ledger
// convoPrompt 的 effects 清单加对应键，并写明「额度由引擎卡死，硬编无用」
```

**铁律**：任何模型给的好处都必须**先过额度再落账**，否则长局数值必崩。

### 4.4 子系统 C：时间/经济层扩展

- 季节/节庆：在 `dateOf` + `advanceTime` 的月循环里插判定（`Math.floor(S.months%12)`），产出写入 `S.engineNews`（`news()`）由模型叙述。
- 经营/产业：`S.assets[]` + 在 `advanceTime` 里按 `n` 个月结算收益（照 `sectStipend`/`sectTick` 写法），坏事件走 `makeDoom` 风格先定死。
- 债务：新增 `S.debts[]`，在 `upkeepPerMonth` 里并入月开销，在 `renderPanel` 生计块显示。

### 4.5 子系统 D：纯 UI 层扩展（零裁定）

最省事的加法：新页签（`#tabs` + `.tabpane` + `renderXxx()`），数据全部从 `S` 读。可以做：关系图（已有 `renderGraph` 可抄）、时间线（用 `S.ledger` 渲染）、章节目录（`bookAll(S.runId)` 取 IndexedDB）、统计页（`S.stats`）。

### 4.6 别忘了给新子系统留「模型侧防呆」

| 风险 | 对策（照抄现有写法） |
|---|---|
| 模型返回对象而不是字符串 | `plain()` |
| 模型返回字符串而不是数组 | `asArr()` |
| 数字越界 | `clamp` / `clampW` / `pw` |
| 把等级写成「精通」 | 仿 `skillLevelFromText` 加文本→数值映射 |
| 提到不存在的 NPC | 一律 `findNpc(name)` 命中才生效（现有全部更新都这么做） |
| 给主角加不存在的物品类别 | `const cat=(p.items[it.cat]!==undefined)?it.cat:'其他'` |
| 一回合塞太多好事 | 全局上限常量（如 `MAX_NEW_PER_TURN=2`）+ 在 `applyTurn` 切片 |

---

## 5. 存档迁移：每次加子系统都要做的 4 步

```js
// 1) 版本号 +1
const SAVE_VERSION=7;
// 2) 新字段进骨架
function newStateShell(){ return { v:SAVE_VERSION, /*…*/ 新字段: 默认值 }; }
// 3) migrate 加分支（放在最后，注意现有分支里有 v<4 直接跳 v=5 的写法，别被带偏）
if(s.v<7){ s.新字段 = s.新字段||默认值; verifySubsystem(s); s.v=7; }
// 4) 老存档补数据表：任何“由旧字段推导”的迁移都要在 migrate 里做，不要指望运行时兜底
```

**迁移检查单**：
- [ ] `migrate` 幂等（可重复调用不破坏数据）
- [ ] `loadGame` 对 v 落后的档给提示（现在静默升级）
- [ ] `exportBook` 的 markdown 表头包含新栏目
- [ ] `chapterText`/`logChapter` 对新区块（如比武块、对话块）有导出路径
- [ ] localStorage 超限降级链（`saveGame` 里砍 `chapters` → 清 `chapters` → toast）**保持不变**
- [ ] 换题材时**换存储键前缀**（`wuxia_*` → 新前缀），否则老档会串进新题材

---

## 6. 换题材后的提示词回归测试（必做）

用一个固定种子跑 10 回合，逐项核对：

| 检查项 | 期望 |
|---|---|
| 是否出现旧题材词汇 | 0 次（在 `turnPrompt` 输出里 grep 旧词表） |
| 是否出现科幻/玄幻越界词（或新铁律禁词） | 0 次 |
| 是否自行写年月 | 0 次（应只说“这些日子”） |
| 是否在剧情里直接写出战斗胜负 | 0 次（应给 `duel` 字段） |
| 篡改判定 | `narrative` 与 `judgeBlock` 结论一致率 100% |
| 姓名一致性 | `fixNames` 触发修正后，自称/旁称姓氏均为主角真姓 |
| 相貌一致性 | 描写与 `AV_DESC[slot]` 不冲突 |
| 数值膨胀 | 单回合`武功`增量 ≤ `growthCap()*months`；`money` 增量 ≤ `600*months*money系数` |
| 新子系统字段 | 未填时 `applyTurn` 不报错（全部 `num()`/存在性判断兜底） |

**长局压测**：连跑 200 回合，观察 ①`volumes` 是否持续增长、②`ledger` 是否被正确裁剪、③localStorage 是否触发降级、④`rankRefresh` 后榜单是否仍有活人、⑤`S.npcs` 数量是否失控（必要时加 NPC 上限与合并策略）。

---

## 7. 分阶段落地顺序与工作量

| 阶段 | 内容 | 改动面 | 相对工作量 |
|---|---|---|---|
| P0 | 抽适配器 + `mock` 适配器 + 设置面板扩字段 | `callLLM`/`llmJSON`/`cfg` 三处 | 小 |
| P1 | 词汇映射全量替换（含 UI 文案） | 全局查找替换 + 数据表 | 中（量大但机械） |
| P2 | `WORLD_RULES`/`STYLE_SYSTEM`/`FREEDOM` 三块重写 | 3 个常量 | 中（最需要反复调） |
| P3 | `STANCES/TACTICS/duelRound/oppProfile` 战斗改造 | 战斗段 | 中大 |
| P4 | 数据表重建（头像/名单/伤残/事件/成就/出身） | 表数据 | 中 |
| P5 | 新子系统（按 §4 模板，一次一个） | 7 处联动 | 每个子系统中 |
| P6 | 存档迁移 v7 + 回归测试 + 长局压测 | `migrate`/测试 | 小到中 |

**优先级建议**：P0 与 P1 先做（立刻可见效果，且不破坏裁定层）；P2 反复磨；P3 若题材差异大必须做；P5 一次只加一个并按 §4.6 补防呆。

---

## 8. 三条不可动摇的设计原则（改到哪都要守）

1. **判定权在引擎**：模型只叙事。任何“让模型决定成败/生死/数值”的改动，都会让长局不可控。
2. **既成事实进台账**：`ledger()` 记录的事永不进压缩、永不被摘要吃掉，且回灌提示词时必须带「不得与之矛盾」的措辞。
3. **未命中即忽略**：模型返回的 NPC 名、物品类别、宿命名，一律 `findNpc` / 白名单命中才生效，绝不因“模型说了”就新建数据。
